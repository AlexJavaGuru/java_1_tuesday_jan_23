Является ли следующий интерфейс функциональным?

Нет, поскольку содержит два метода. Если бы одна из сигнатур этих методов совпадала
 с сигнатурой метода класса Object, т.к. все классы считаются унаследованными
 от класса Object в java (например, методы equals() или toString()), то интерфейс
 можно было бы считать функциоанльным.

interface ProductDatabase {

    void save(Product product);

    Product findByTitle(String productTitle);
}



class Product {

    private String title;

    public Product(String title) {
        this.title = title;
    }

    public String getTitle() {
        return this.title;
    }
}

